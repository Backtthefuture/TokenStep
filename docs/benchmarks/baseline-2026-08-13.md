# v0.1.48 基线验收记录（G-E0 / E0-T04 前置，2026-08-13）

## 环境

| 项 | 值 |
|---|---|
| commit | `ef13871`（origin/main = HEAD，v0.1.48） |
| 系统 | macOS 15（darwin 24.6.0），Apple Silicon arm64 |
| 工具链 | CLT only（`/Library/Developer/CommandLineTools`），Swift 6.1.2；**无完整 Xcode** |
| 工作区 | tracked dirty = 0（基线纯净） |

## 自动验收结果

| 命令 | 结果 | 备注 |
|---|---|---|
| `./script/test_ccswitch_proxy_collector.sh` | ✅ PASS | swiftc+overlay 编译 fixture，本机可跑 |
| `./script/test_codex_cumulative_collector.sh` | ✅ PASS | 同上 |
| `./script/test_usage_recalibration_migration.sh` | ✅ PASS | 同上 |
| `python3 script/check_localization.py` | ✅ PASS | |
| `python3 script/check_language_refresh.py` | ✅ PASS | |
| `./script/benchmark_energy_efficiency.sh` | ✅ PASS | 数字见下 |
| `swift test --package-path TokenStepSwift` | ⛔ **本机无法运行（环境限制）** | 原始错误：manifest 编译失败 `Undefined symbols for architecture arm64: "PackageDescription.Package.__allocating_init(...)"`（CLT 6.1.2 无 Xcode；CI macos-15 正常执行同一命令） |
| `./script/build_swiftui_and_run.sh --no-launch` | ⏳ 待跑 | 排在 E0 收尾全量验收（构建耗时较长，避免与基准采集同轮） |

**swift test 的替代证据与未验 gate**（按 PRD §4.7）：三个 fixture 脚本以 swiftc+overlay 编译同一份源码子集并断言采集口径，等效覆盖 collector 正确性；XCTest 全量套件以 CI（macos-15）为准，在 E0 变更的 PR 上验证，此处**不默认为通过**。

## 能耗基准数字（本机真实数据，warm 模式）

```
state_files=1263          state_ms=69
collect_ms=53713          ← warm 采集 53.7s（缓存命中路径）
cache_generation=1        cached_sessions=1107   cached_records=68233
max RSS ≈ 135.8 MiB (142458880 B)
logical writes ≈ 49.4 MiB (51760040 B)
instructions retired ≈ 1.10T
```

## 观察（仅记录，不下结论）

1. **warm 采集 53.7s**：距 Helper 120s 超时余量偏小；cached_records 68k 条。是否构成"有证据的缺口"待 E0-T04 固定语料（cold/warm/append/损坏 + Claude 侧）后按 PRD §4.6 三分类裁决。
2. **基准语料覆盖缺口**：现行 `benchmark_energy_efficiency.sh` 仅 warm 单模式，未覆盖 PRD §4.6 要求的 Codex cold/append、Claude cold/warm/append、损坏缓存/截断行场景——这是 E0-T04 待补的基准工具缺口（allowed_paths 内）。
