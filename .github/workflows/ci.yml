name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Build TokenStep
        run: ./script/build_swiftui_and_run.sh --no-launch
